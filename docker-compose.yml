version: '3'
services:
  nexus:
    image: "sonatype/nexus3"
    container_name: nexus
    restart: always
    volumes:
      - 'nexus-data:/nexus-data'
  nginx:
    image: "nginx"
    container_name: nginx
    ports:
      - "443:443"
      - "5000:5000"
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./private.key:/etc/nginx/
      - ./cert.crt:/etc/nginx/
    depends_on:
      - nexus
volumes:
  nexus-data:
