version: '3'
services:
  nexus:
    image: "sonatype/nexus3"
    container_name: nexus
    restart: always
    volumes:
      - 'nexus-data:/nexus-data'
  nginx:
    build: .
    container_name: nginx
    ports:
      - "443:443"
      - "5000:5000"
    restart: always
    depends_on:
      - nexus
volumes:
  nexus-data:
    external: true
